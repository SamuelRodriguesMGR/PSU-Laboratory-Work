
using System;



namespace MyConsoleApp
{
    class Program
    {
        private static void Main(string[] args)
        {
            Numbers numbers = new Numbers();

            // Console.WriteLine(numbers.sumLastNums(4568));

            // Console.WriteLine(numbers.isPositive(3));
            // Console.WriteLine(numbers.isPositive(-5));

            // Console.WriteLine(numbers.isUpperCase('D'));
            // Console.WriteLine(numbers.isUpperCase('q'));

            // int n = numbers.lastNumSum(5, 11);
            // n = numbers.lastNumSum(n, 123);
            // n = numbers.lastNumSum(n, 14);
            // n = numbers.lastNumSum(n, 1);
            // Console.WriteLine(n);



            // Console.WriteLine(numbers.safeDiv(5, 0));
            // Console.WriteLine(numbers.safeDiv(8, 2));

            // Console.WriteLine(numbers.makeDecision(5, 7));
            // Console.WriteLine(numbers.makeDecision(8, -1));
            // Console.WriteLine(numbers.makeDecision(4, 4));

            // Console.WriteLine(numbers.sum3(5, 7, 2));
            // Console.WriteLine(numbers.sum3(8, -1, 4));

            // Console.WriteLine(numbers.age(5));
            // Console.WriteLine(numbers.age(31));
            // Console.WriteLine(numbers.age(44));

            // numbers.printDays("четверг");
            // numbers.printDays("чт");



            // Console.WriteLine(numbers.reverseListNums(5));

            // Console.WriteLine(numbers.pow(2, 5));

            // Console.WriteLine(numbers.equalNum(1111));
            // Console.WriteLine(numbers.equalNum(1211));

            // numbers.leftTriangle(2);
            // numbers.leftTriangle(4);

            // numbers.guessGame();



            // Console.WriteLine(numbers.findLast([1, 2, 3, 4, 2, 2, 5], 2));

            // Console.WriteLine(numbers.add([1, 2, 3, 4, 5], 9, 3));

            // numbers.reverse([1, 2, 3, 4, 5]);

            // Console.WriteLine(numbers.concat([1, 2, 3], [7, 8, 9]));
            
            // Console.WriteLine(numbers.deleteNegative([1,2,-3,4,-2,2,-5]));
            
        }
    }
}
