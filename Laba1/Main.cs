
using System;



namespace MyConsoleApp
{
    class Program
    {
        private static void Main(string[] args)
        {
            // Method2.Launch();
            // Method4.Launch();
            // Method6.Launch();
            // Method8.Launch();
            // Method10.Launch();

            // Terms2.Launch();
            // Terms4.Launch();
            // Terms6.Launch();
            // Terms8.Launch();
            // Terms10.Launch();

            // Cycle2.Launch();
            // Cycle4.Launch();
            // Cycle6.Launch();
            // Cycle8.Launch();
            // Cycle10.Launch();

            // Array2.Launch();
            // Array4.Launch();
            // Array6.Launch();
            // Array8.Launch();
            Array10.Launch();
        }
    }
}
